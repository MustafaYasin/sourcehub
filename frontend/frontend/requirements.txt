# Core application dependencies
streamlit==1.31.0
openai==1.63.2
python-dotenv==1.0.1

# Database dependencies - Note the change to psycopg2-binary
sqlmodel==0.0.22
pgvector==0.3.6
psycopg2-binary==2.9.9  # This is the key change - using the pre-compiled binary version

# Document processing
langchain==0.3.19
langchain-community==0.3.18
langchain-text-splitters==0.3.6
unstructured==0.16.23
docx2txt==0.8
pypdf==5.3.0
tiktoken==0.8.0

# Your database package - this should come last to ensure all dependencies are installed first
git+https://github.com/mustafayasin/sourcehub.git#subdirectory=database